#ifndef BUBBLESORT_LL_HPP
#define BUBBLESORT_LL_HPP

#include "LinkListHandling.hpp" // Ensure it is only included once

// Function prototype for Bubble Sort on linked list
void bubbleSort(Article*& head);

// Function prototype for extracting the year from the date
int extractYear(const std::string& date);

#endif // BUBBLESORT_LL_HPP
